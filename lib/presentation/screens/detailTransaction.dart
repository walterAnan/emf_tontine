import 'package:flutter/material.dart';
class DetailTransaaction extends StatelessWidget {
  const DetailTransaaction({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text(
        'Page de details d\'une transaction'
      ),),
    );
  }
}
