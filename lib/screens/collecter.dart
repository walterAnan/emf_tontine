import 'package:flutter/material.dart';


class Collecter extends StatelessWidget {
  const Collecter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Page de Collecte')),
    );
  }
}
